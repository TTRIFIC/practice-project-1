# React Guide Practice Project 1

### A simple app that takes in a user's name and age and adds them to the list of users below the form. This app also will show an Error Modal if the inputs are invalid.
